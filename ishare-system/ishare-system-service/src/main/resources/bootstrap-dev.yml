spring:
  cloud:
    nacos:
      config:
        server-addr: localhost:8848
        file-extension: yml
        ext-config:
          - dataId: application.yml
            group: DEFAULT_GROUP
            refresh: true
      discovery:
        server-addr: localhost:8848